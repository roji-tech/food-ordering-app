import React from "react";

const Checkout = ({ show }) => {
  return (
    <CheckoutStyle show={show}>
      <div></div>
      <div id="main">Checkout</div>
    </CheckoutStyle>
  );
};

export default Checkout;
